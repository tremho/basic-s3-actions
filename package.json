{
  "name": "@tremho/basic-s3-actions",
  "version": "2.0.0-pre-release.6",
  "description": "",
  "main": "build/S3Actions.js",
  "types": "build/S3Actions.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "clean": "rm -fr build node_modules && npm install",
    "build": "tsc && cp logconfig.json build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "s3",
    "AWS",
    "CRUD"
  ],
  "author": "tremho",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.529.0",
    "@tremho/gen-logger": "^2.0.0-pre-release.4",
    "fast-xml-parser": "^4.4.1"
  },
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0",
    "@types/node": "^22.0.1"
  }
}